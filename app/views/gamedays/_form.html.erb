<%= form_for(@gameday) do |f| %>
  <% if @gameday.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@gameday.errors.count, "error") %> prohibited this gameday from being saved:</h2>

      <ul>
      <% @gameday.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :number %><br>
    <%= f.number_field :number %>
  </div>
  <div class="field">
    <%= f.label :league_id %><br>
    <%= f.select(:league_id, options_for_select([["Bundesliga", 1], ["Premier League", 2]])) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
